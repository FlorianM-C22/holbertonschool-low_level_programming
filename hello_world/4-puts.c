#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Prints a message with the puts command'
 * Return: Always 0 (Success)
 */
int main(void)
{

    char myStr[] = "\"Programming is like building a multilingual puzzle";
    puts(myStr);
    return (0);

}
