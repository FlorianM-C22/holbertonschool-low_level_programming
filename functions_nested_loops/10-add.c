#include "main.h"
#include <stdio.h>

/**
 *add - function that adds two integers and returns the result
 *a - value 1
 *b - value 2
 * Return: 0 success
 */
int add(int a, int b)
{
	return (a + b);
}
